# Spec -- <Feature Name>

## Overview
One paragraph: what this feature does and why it exists.

## Out of Scope
Explicit list of what this feature does NOT do.

## Public API Surface
<!-- List new or modified public symbols in a code block using the project's
     language. Include types, parameters, defaults, and which module each
     belongs to. -->

## Behaviors

<!-- Each behavior maps to one test class in Phase 6. Express behaviors as
     test class stubs in the project's language. The class docstring/comment
     defines the contract (REQUIREMENT/WHO/WHAT/WHY + MOCK BOUNDARY). Each
     test method stub defines one scenario.

     WHAT count = method count. Every numbered WHAT clause maps to exactly
     one test method. A WHAT clause without a method is a coverage gap.
     A method without a WHAT clause is speculative.

     For the language-specific behavior example, read the appropriate
     reference file from this skill:
       - references/spec/python.md
       - references/spec/typescript.md
       - references/spec/java.md
       - references/spec/csharp.md
-->

### <BehaviorName>

<!-- Insert test class stub here using the pattern from your language's
     spec reference file. The stub must include:
     1. Class-level contract: REQUIREMENT / WHO / WHAT / WHY + MOCK BOUNDARY
     2. One method stub per WHAT clause with Given / When / Then docstring
     3. Method bodies as ... / throw / TODO -- no implementation -->

<!-- Repeat for each distinct behavioral requirement. -->
